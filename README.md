simple python script utilizing pyautogui
Default settings only work for accepting queue pops on a 1920x1080 display and a 1280x720 league client.
If you have different resolutions, use posfinder.py to determine the position of the accept button, and replace the default pos_x and pos_y values in 1auto_accept.py with the new x and y values from posfinder.py
